describe('landing page', () => {
    let test = 'hello'
    beforeEach('route to base url', () => {
        cy.visit('/')
        cy.viewport(1100,720)
    })
    it('find the search button', () => {
        
    })
})